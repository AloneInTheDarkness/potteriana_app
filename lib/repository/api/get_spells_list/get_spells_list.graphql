query Spells($after: String){
    spells(after: $after) {
        totalCount
        pageInfo{
            endCursor
        }
        nodes {
            slug
            image
            name
        }
    }
}